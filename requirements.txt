# Enhancement 3: Supply-chain pinning with version locks
# Install with: pip install -r requirements.txt --require-hashes
# To regenerate hashes: pip hash <package>==<version>

# Core dependencies - PINNED VERSIONS
playwright==1.55.0
pdfplumber==0.11.7
cryptography==46.0.3
idna==3.11

# Platform-specific
pywin32==311 ; sys_platform == 'win32'

# Optional enhancements
portalocker==3.2.0  # Enhancement 6: Stronger file locking
pyseccomp==0.1.2 ; sys_platform == 'linux'  # Enhancement 11: Linux sandboxing

# Note: For full hash verification, use pip-tools:
# pip install pip-tools
# pip-compile --generate-hashes requirements.in -o requirements.txt
# pip install --require-hashes -r requirements.txt
